/*
 * Test case Name  : shbtree-load-then-create-index_03.aql
 * Description     : load data to primary index followed by shbtree index creation followed by inserts, then a query. 
 * Expected Result : Success
 * Date            : Sept 30 2015
 */

use dataverse STBench;
count(
for $x in dataset SimpleGeoPlace
let $n :=  create-rectangle(point("-2.433658,53.0942802"), point("8.433658,54.0942802"))
where spatial-intersect($x.coordinates, $n)
return $x
);
