/*
 * Test case Name  : shbtree-create-index-then-load_02.aql
 * Description     : shbtree index creation followed by load data followed by inserts, then a query. 
 * Expected Result : Success
 * Date            : Sept 30 2015
 */
 
use dataverse STBench;
count(
for $x in dataset SimpleGeoPlace
let $n :=  create-rectangle(point("14.433658,45.0942802"), point("14.533658,60.0942802"))
where spatial-intersect($x.coordinates, $n)
return $x
);
